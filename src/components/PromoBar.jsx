const PromoBar = () => {
    return (
        <div className="bg-brand-taupe text-white text-xs text-center py-2 tracking-widest uppercase font-bold">
            Envíos a todo Paraguay 🇵🇾 | Pedidos al WhatsApp
        </div>
    );
};

export default PromoBar;
